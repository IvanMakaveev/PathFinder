export const NODE_TYPES = Object.freeze({
    NORMAL: 'NormalNode',
    BROKEN: 'BrokenNode',
});

export const NODE_TYPE_OPTIONS = Object.freeze([
    NODE_TYPES.NORMAL,
    NODE_TYPES.BROKEN,
]);

export const NODE_MODIFIER_TYPES = Object.freeze({
    COOLANT: 'CoolantNode',
    SPECIALIZED: 'SpecializedNode',
});

export const NODE_MODIFIER_TYPE_OPTIONS = Object.freeze([
    NODE_MODIFIER_TYPES.COOLANT,
    NODE_MODIFIER_TYPES.SPECIALIZED,
]);